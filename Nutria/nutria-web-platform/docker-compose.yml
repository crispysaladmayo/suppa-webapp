services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: nutria
      POSTGRES_PASSWORD: nutria
      POSTGRES_DB: nutria
    ports:
      - "5433:5432"
    volumes:
      - nutria_pg:/var/lib/postgresql/data

volumes:
  nutria_pg:
